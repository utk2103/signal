[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "laya-mlx"
version = "0.1.0"
description = "Native MLX inference for Laya typed decision models on Apple silicon"
keywords = ["mlx", "laya", "typed-decisions", "decision-model", "local-ai", "apple-silicon"]
classifiers = [
  "Development Status :: 4 - Beta",
  "Intended Audience :: Developers",
  "Operating System :: MacOS",
  "Programming Language :: Python :: 3",
  "Topic :: Scientific/Engineering :: Artificial Intelligence",
]
readme = "README.md"
requires-python = ">=3.11"
license = "Apache-2.0"
dependencies = [
  "mlx>=0.32.2,<0.33; sys_platform == 'darwin' and platform_machine == 'arm64'",
  "numpy>=1.26",
  "huggingface-hub>=0.34,<2",
  "tokenizers>=0.21,<1",
]

[project.optional-dependencies]
dev = ["pytest>=8", "ruff>=0.12", "build>=1"]
reference = ["torch>=2.14,<3", "transformers>=5.17,<6", "safetensors>=0.6"]
benchmark = ["matplotlib>=3.10", "pyarrow>=18"]
demo = ["rich>=15,<16", "Pillow>=12,<13"]
trex = ["pygame-ce>=2.5.8,<3", "httpx>=0.28,<1", "imageio-ffmpeg>=0.6,<1"]
social = ["httpx>=0.28,<1"]
live = ["httpx>=0.28,<1", "playwright>=1.48,<2"]

[project.scripts]
laya-mlx = "laya_mlx.cli:main"
laya-snake = "laya_mlx.snake.cli:main"
laya-trex = "laya_mlx.trex.cli:main"
laya-social = "social.cli:main"

[project.urls]
Repository = "https://github.com/utk2103/laya-vs-jev"
Upstream = "https://github.com/NandhaKishorM/laya"
Documentation = "https://github.com/utk2103/laya-vs-jev#readme"
Issues = "https://github.com/utk2103/laya-vs-jev/issues"
Benchmarks = "https://github.com/utk2103/laya-vs-jev/blob/main/BENCHMARKS.md"

[tool.hatch.build.targets.wheel]
packages = ["laya_mlx", "social", "social.live"]

[tool.hatch.build.targets.sdist]
include = ["/laya_mlx", "/social", "/pyproject.toml", "/README.md", "/LICENSE", "/NOTICE"]

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = ["integration: requires downloaded Laya checkpoints"]

[tool.ruff]
line-length = 100
extend-exclude = [".upstream"]

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F", "I"]
